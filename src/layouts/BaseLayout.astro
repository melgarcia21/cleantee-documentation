---

import '../styles/global.css';

interface Props{
	title: string;
	description?: string;
	ogImage?: string;
}

const {
	title,
	description = 'Complete documentation for CleanTee Online Booking System - A modern web application for cleaning service management',
	ogImage = '/images/og-image.png',
} = Astro.props;

const canonicalURL = new URL(Astro.url.pathname, Astro.site);
const fullTitle = title ? `${title} | CleanTee Docs` : 'CleanTee Documentation';

---